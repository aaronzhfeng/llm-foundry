# Qwen3 Serving System Requirements
# Install: pip install -r requirements.txt

# Core serving
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# ML dependencies (should already be installed from training)
torch>=2.1.0
transformers>=4.35.0

# Optional: for production deployment
# gunicorn>=21.0.0
# python-multipart>=0.0.6

